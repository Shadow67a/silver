#ifndef LIB_HPP
#define LIB_HPP

#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>
#include <glm/gtc/type_ptr.hpp>

#include <ft2build.h>
#include FT_FREETYPE_H

#include <GLFW/glfw3.h>

struct Gradient
{
	glm::vec3 x;
	glm::vec3 y;
	float angle;
};

namespace silver
{
	class App;
	class View;
	class Shader;

	class Triangle;
	class Quad;
	class Image;
	struct Font;
	class Text;

	extern glm::mat4 view;
	extern glm::mat4 projection;
	extern int windowWidth;
	extern int windowHeight;
	extern float iTime;
	extern glm::vec4 cursor;
	extern FT_Library ft;
	

	glm::vec3 hex(int color);
	Gradient linearGradient(glm::vec3 x, glm::vec3 y, float angle);
};

#endif
